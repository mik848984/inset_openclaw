mkdir CenteredAuthLayout && mv CenteredAuthLayout.tsx CenteredAuthLayout/route.tsx && 
mkdir DefaultAuthLayout && mv DefaultAuthLayout.tsx DefaultAuthLayout/route.tsx && 
mkdir PricingAuthLayout && mv PricingAuthLayout.tsx PricingAuthLayout/route.tsx

mv forgot-password ../../app/auth && mv lock ../../app/auth && mv main ../../app/auth && mv sign-in ../../app/auth && mv sign-up ../../app/auth && mv verification ../../app/auth 


mkdir centered && mv centered.tsx centered/route.tsx && 
mkdir default && mv default.tsx default/route.tsx 
